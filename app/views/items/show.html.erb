<div class="container">
  <div class="row">
    <div class="span6 offset3">
      <h2><%= @item.name %></h2>
      <ul>
        <li><%= @item.reserved_price %></li>
        <li>Sold: <%= @item.sold ? "yes" : "no" %></li>
      </ul>
    </div>
  </div>
</div>